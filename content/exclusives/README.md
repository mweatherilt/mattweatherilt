# Exclusives

One markdown file per YouTube long-form Exclusive.

Frontmatter should include `title`, `date`, `slug`, `excerpt`, and `tags`. Optional `members: true` keeps a draft off public routes until the video is public.

Public routes: `/exclusives/{slug}/`. Homepage hero, listing, sitemap, and RSS skip `members: true`.

Create-only: never delete an Exclusive file.
